# 复制本文件为 `.env` 后填写（`.env` 已在 .gitignore 中，勿提交）
# 重启 `npx expo start` 后生效。
#
# --- 方式 A：服务端代理（用户免填 Key，推荐上架）---
# 部署 Netlify 后在站点环境变量配置 DASHSCOPE_API_KEY（勿用 EXPO_PUBLIC_）。
# 此处填你的站点上的代理地址，例如：
# EXPO_PUBLIC_CHAT_PROXY_URL=https://你的站点.netlify.app/api/chat
# 或与 netlify.toml 一致：/api/chat -> dashscope-proxy 函数
# 若 Netlify 设置了 APP_SHARED_SECRET，此处需一致：
# EXPO_PUBLIC_CHAT_PROXY_SECRET=
#
# --- 方式 B：直连 DashScope（开发或自备 Key）---
# 阿里云 DashScope：https://dashscope.console.aliyun.com/
EXPO_PUBLIC_DASHSCOPE_API_KEY=
# EXPO_PUBLIC_OPENAI_API_KEY=
